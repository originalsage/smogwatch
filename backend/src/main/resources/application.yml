server:
  port: 8080
  # Unwrap X-Forwarded-* / Forwarded so getRemoteAddr() reflects the client behind ingress.
  forward-headers-strategy: framework

spring:
  datasource:
    url: jdbc:mysql://smogwatch-db-mysql.ns-ae2uyc2d.svc:3306/smogwatch?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: hjgkk5sk
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.smogwatch.entity
  configuration:
    map-underscore-to-camel-case: true

baidu:
  map:
    ak: FZYrpESUYUG5XvT7Q2RBvFdlyitk1Vdi
    location-url: https://api.map.baidu.com/location/ip
    weather-url: https://api.map.baidu.com/weather/v1/
    timeout-ms: 5000
    retry-times: 2

app:
  cors:
    allowed-origins: "*"
  district:
    csv-path: classpath:weather_district_id.csv
